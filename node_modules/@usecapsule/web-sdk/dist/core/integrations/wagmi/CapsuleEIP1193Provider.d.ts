import { EIP1474Methods, EIP1193Provider, EIP1193RequestFn } from 'viem';
import { EventEmitter } from 'eventemitter3';
import { Chain } from '@wagmi/chains';
import { CoreCapsule } from '../../CoreCapsule';
interface CapsuleEIP1193ProviderOpts {
    capsule: CoreCapsule;
    chainId: string;
    chains: Chain[];
    disableModal?: boolean;
    appName: string;
    storageOverride?: Pick<Storage, 'setItem' | 'getItem'>;
    renderModal?: (capsule: CoreCapsule, appName: string, onClose: () => void) => void;
}
export declare class CapsuleEIP1193Provider extends EventEmitter implements EIP1193Provider {
    private currentHexChainId;
    private walletClient;
    private chainTransportSubscribe?;
    private chains;
    private capsule;
    private disableModal;
    private appName;
    private storage;
    private renderModal?;
    constructor(opts: CapsuleEIP1193ProviderOpts);
    private getStorageChainId;
    private setChainId;
    private getRpcUrlsFromViemChain;
    private wagmiChainsToAddEthereumChainParameters;
    private accountFromAddress;
    private setCurrentChain;
    request: EIP1193RequestFn<EIP1474Methods>;
}
export {};
