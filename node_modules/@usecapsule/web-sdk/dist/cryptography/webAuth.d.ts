import { Environment } from '../core/definitions';
export declare function parseCredentialCreationRes(creds: any, algorithm: number): {
    cosePublicKey: string;
    clientDataJSON: string;
};
export declare function createCredential(env: Environment, userId: string, email: string): Promise<{
    creds: any;
    userHandle: Uint8Array;
    algorithm: number;
}>;
export declare function generateSignature(env: Environment, challenge: string, allowedPublicKeys: string[]): Promise<any>;
