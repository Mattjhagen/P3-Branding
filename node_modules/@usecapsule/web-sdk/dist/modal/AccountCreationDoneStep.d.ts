import { ModalStep } from './steps';
import { Capsule } from '../Capsule';
import { CoreCapsule } from '../core/CoreCapsule';
export declare function AccountCreationDoneStep({ currentStep, appName, onClose, capsule, rampNetworkApiKey, defaultAsset, onRampAvailable, }: {
    currentStep: ModalStep;
    appName: string;
    onClose: () => void;
    capsule: Capsule | CoreCapsule;
    rampNetworkApiKey: string;
    defaultAsset: string;
    onRampAvailable: boolean;
}): JSX.Element;
