import { Ctx } from '../core/definitions';
export declare function keygen(ctx: Ctx, userId: string, secretKey: string | null, skipDistribute: boolean, customFunction: (params?: any) => void, sessionCookie?: string): Promise<{
    signer: string;
    walletId: string;
    recoveryShare: string | null;
}>;
